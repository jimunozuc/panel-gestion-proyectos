PORT=3001

# Secreto compartido que debe enviar Power Automate en el header x-refresh-secret
# al llamar POST /api/webhook/refresh. Si se deja vacío, el endpoint queda sin protección.
REFRESH_SECRET=

# Cadena de conexión a Postgres, ej: postgres://localhost:5432/panel
# Si se deja vacía, el panel sigue funcionando en modo solo lectura desde el Excel.
DATABASE_URL=

# Origen exacto del frontend (para permitir la cookie de sesión entre dominios).
CORS_ORIGIN=http://localhost:5173

# Secreto para firmar la cookie de sesión. Cambiar por uno propio en producción.
COOKIE_SECRET=
