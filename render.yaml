services:
  - type: web
    name: panel-gestion-proyectos-backend
    runtime: node
    rootDir: backend
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: REFRESH_SECRET
        sync: false
